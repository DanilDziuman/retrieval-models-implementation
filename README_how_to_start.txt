** THIS IS A LEARNING REPOSITORY, NOT A LIBRARY OR COMMERCIAL CODE **

1. Open the terminal or command prompt and navigate to the directory "Lab1_document-models".
2. Run "npm install" to download and install the dependencies.
3. Run the server side using the script defined in 'package.json': "npm start"
4. Visit the website as a client on your local browser: localhost:3000
